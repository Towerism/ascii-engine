language: cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
      - gcc-5

install: ci/install.sh

script: make coverage

after_success: ci/upload_coverage.sh
