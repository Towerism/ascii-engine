language: cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
      - gcc-5

install:
  - ci/install_to_custom_path.sh
  - export PATH=$PWD/usr/bin:$PATH

script: make coverage

after_success: coveralls-lcov build/coverage/coverage.info.cleaned
